version: '3.7'

services:
  loki:
    image: grafana/loki:2.8.2
    command: --config.file=/etc/loki/local-config.yaml
    ports:
      - "3100:3100"

  grafana:
    image: grafana/grafana:9.5.0
    ports:
      - "3000:3000"
    depends_on:
      - loki